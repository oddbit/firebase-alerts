import {describe, it} from "mocha";
import {expect} from "chai";

describe("functions/test/crashlytics/functions.spec.ts", () => {
  describe("onNewAnrIssuePublished", () => {
    it("should just return true", async () => {
      expect(true).to.be.true;
    });
  });
});
